/* ============================================================
   Переключатель режима работы.

   VITE_USE_MOCKS=true  -> демо на мок-данных (как раньше).
                           Ничего не сломается, если бэкенд лежит.
   VITE_USE_MOCKS=false -> боевой режим: данные из API.

   Задаётся в .env.local (см. .env.example).
   ============================================================ */

export const USE_MOCKS = import.meta.env.VITE_USE_MOCKS !== 'false'
